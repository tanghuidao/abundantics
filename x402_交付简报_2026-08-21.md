# x402 推理端点每日归档 · 交付简报（x402-archive v1.0）

> 对象仓库：github.com/tanghuidao/token-parity（main）
> 归档日期：2026-08-21 | 简报日期：2026-08-21
> 主源实测免 key、免登录、免注册，未发生任何链上交互。

---

## 1. 数据源探测结果

### 1.1 Coinbase CDP x402 Bazaar（任务书第 1 源）

| 项目 | 实测结果 |
|---|---|
| URL | `https://api.cdp.coinbase.com/platform/v2/x402/discovery/resources?type=http&limit=N` |
| 免 key / 免登录 | ✅ 是（公开 JSON，无鉴权头） |
| 返回结构 | `{"items":[{...}], "pagination":{..., "total":N}}`，每条含资源描述、URL、类型等 |
| 全量 | **15,165 条** |
| 分页 | 每页上限 100（实测 limit=100 生效）→ 全量需 **152 次请求、约 15MB/日** |
| 结论 | 与红线「单次运行 ≤10 请求」和「日增几十至几百 KB」体量假设严重冲突 → **降级为一次性探测记录**（脚本中保留为失败降级时的可达性探针，不用于归档数据） |

### 1.2 Agentic.Market（任务书第 2 源，最终主源）

| 项目 | 实测结果 |
|---|---|
| URL | `https://api.agentic.market/v1/services?limit=200&offset=N` |
| 免 key / 免登录 | ✅ 是（公开 JSON，无鉴权头） |
| 返回结构 | `{"services":[{id,name,description,domain,category,networks,enriched,endpoints:[{url,method,description,pricing:{amount,currency,network,scheme,maxAmount,minAmount},...}]}], "total":N, "limit":N, "offset":N}` |
| 全量 | **2,310 个服务** |
| 分页 | 每页上限 200（limit=500/1000 实测均被钳回 200）→ 全量需 **12 次请求** |
| 附加探测 | 无 curated/类目过滤参数；返回字段中**不含收款地址与上架时间**（源未披露，不虚构） |
| 结论 | 采用为**每日归档主源**。12 次/日超出任务书「≤10」红线 2 次，**项目所有者已确认接受**；12 次均为同一公开 JSON 接口的标准分页，非爬站式抓取 |

### 1.3 降级路径（两源均不可用时）

脚本写入 `fetch_status: "failed"` 的最小文件，内含当日两源可达性探针（HTTP 状态），**绝不复制昨日数据冒充当日**；本次交付未触发该路径。

---

## 2. 改动文件清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `x402_archive.py` | 新增 | 独立归档脚本（约 250 行），含完整注释与失败降级 |
| `.github/workflows/x402_daily.yml` | 新增 | 每日 UTC 00:20 定时 + 手动触发，抓取→写文件→自动提交 |
| `raw_x402/2026-08-21.json` | 新增 | 当日归档，3.92 MB |
| `parity_index.py` / `build_site.py` / `daily.yml` | **未改动** | 主管线零触碰，符合任务书范围 |
| `parity_series.csv` / 网页 | **未出现** | x402 数据不进任何指数、不上任何网页 |

远端提交（均在 main，GitHub API 已核实）：
- `ddf6140` — x402 archive v1.0：脚本 + workflow + 当日归档
- `1aae76a` — x402 archive 2026-08-21（workflow 自动提交当日数据，与主管线 daily.yml 模式一致）

---

## 3. 当日归档样例（前 30 行）

```json
{
 "archive_date": "2026-08-21",
 "fetched_at": "2026-08-21T08:27:46Z",
 "source": "agentic_market",
 "source_endpoint": "https://api.agentic.market/v1/services?limit=200&offset=N",
 "fetch_status": "ok",
 "total_services": 2310,
 "total_endpoints": 29537,
 "pages_used": 12,
 "truncated": false,
 "settlement_assets": [
  "USDC"
 ],
 "services": [
  {
   "id": "api-exa-ai",
   "name": "Exa",
   "category": "Search",
   "domain": "api.exa.ai",
   "networks": [
    "Base",
    "solana:5eykt4usfv8p8njdtrepy1vzqkqzkvdp"
   ],
   "enriched": true,
   "endpoints": [
    {
     "url": "https://api.exa.ai/contents",
     "method": "POST",
     "price_amount": "0.001",
     "price_scheme": "exact"
```

完整文件：`raw_x402/2026-08-21.json`（紧凑 JSON，`ensure_ascii=False`，与 raw/ 归档同约定）。

---

## 4. 端点与计价单位统计

| 指标 | 数值 |
|---|---|
| 服务总数 | 2,310 |
| 端点总数 | **29,537** |
| reasoning 类服务（关键词+类目判定，仅供简报） | **33** |
| 结算资产 | 100% USDC |
| 链分布 | Base 2,309 / Solana 1（链标记 1 条为 Base+Solana 双链） |
| 文件大小 | 3.92 MB（>2MB → WARNING 已按设计触发，只叫不咬） |

### 计价方式分布（决定 R_A 校准可行性的关键数字）

| 计价方式 | 端点数 | 占比 | 含义 |
|---|---|---|---|
| 按次固定价（exact 且披露金额） | 28,396 | **96.1%** | 每次调用固定 USDC 价 |
| 按次设上限（upto + maxAmount） | 474 | 1.6% | 固定价但设上下限 |
| 未披露价格 | 667 | 2.3% | 疑似按用量/token 动态计价 |

原始 scheme 分布：`exact 29,060 / upto 474 / batch-settlement 2 / nvm:erc4337 1`。

**结论：约 96% 端点为「按次固定 USDC 挂牌价」——该目录数据结构对未来用真实机器间成交价校准 R_A 是可行的原料基础。**

### 其他观察

- 类目：2,236 个服务类目为空（自动索引入库），enriched（人工完善）仅 74 个；
- reasoning 类样例：Claude、ChatGPT、DeepSeek、Groq、Google Gemini、Hyperbolic、Venice、llm-fast.hergertsynthora.com 等 33 个。

---

## 5. 全部自主决策列表

1. **数据源取舍**（已请示项目所有者确认）：CDP 全量 152 请求不可行 → 降级为一次性探测记录；Agentic.Market 全量 12 请求（超红线 2 次，已确认接受）。
2. **体量精简（无损）**：币种 100% 为 USDC → 上提为顶层 `settlement_assets` 字段（若未来出现多币种仍会记录集合）；`min/max` 空值（99%+ 为空）省略键；剔除 `description / iconUrl / parameters / quality` 等大字段。
3. **体量仍超 2MB**：精简后 3.92MB/日仍超阈值 → 按任务书「只叫不咬」只打印 WARNING 并继续写入；未进一步压缩（如需可加 gzip 或截断尾部，属后续任务）。
4. **收款地址与上架时间**：源返回结构不含这两字段（探测确认）→ 不虚构，归档字段以源实际披露为准。
5. **reasoning 类判定**：关键词 + 类目匹配，仅用于交付简报统计，**不入档**（分类边界会变，原始截面才保值）。
6. **依赖**：仅 requests（已存在），未引入 BeautifulSoup（无需 HTML 解析）。
7. **Workflow 时间**：UTC 00:20，错开主管线 00:10。
8. **失败策略**：当日重复运行覆盖当日文件；失败写最小探针文件、不重试、不报警、不复制昨日数据（连续失败靠人工翻 Actions 日志，任务书确认的零成本旁路模式）。
9. **自动提交**：每日归档由 workflow 自动提交（与 daily.yml 同模式）→ 本地仓库日常会落后于远端，**手动运行/改脚本前需先 `git pull`**。

---

## 6. 自校验结果（任务书要求，已通过）

- ✅ 手动触发 workflow（run `32463389069`）：6 个步骤全绿（含脚本运行、提交、推送）
- ✅ `raw_x402/2026-08-21.json` 已存在于远端 main（提交 `1aae76a`）
- ✅ 顶层字段齐全：`archive_date` / `source_endpoint` / `fetch_status`
- ✅ 当日归档 JSON 可解析、计数与脚本日志一致（2,310 / 29,537）
- ✅ 主管线未受影响：本次提交仅新增 3 个文件，未触碰 `parity_index.py` / `build_site.py` / `daily.yml`

---

*任务书 v1.0（2026-08-21）全部要求已满足。本模块为独立旁路，不进入任何指数计算，后续使用需另行授权的任务书。*
