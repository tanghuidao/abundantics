# x402 归档压缩补丁 v1.1 交付简报

日期：2026-08-21 ｜ 执行依据：《workbuddy任务_x402归档压缩补丁_v1_1.md》

---

## 一、改动清单与提交哈希

| 文件 | 改动 | 状态 |
|---|---|---|
| `x402_archive.py` | 输出改为 `.json.gz`（标准库 gzip）；体量哨兵阈值改为**压缩后 >1MB** 输出 `::warning::`（GitHub Actions 注解格式）；失败探针文件同样写 `.json.gz`；脚本版本号 1.0 → 1.1；UA 字符串同步更新 | 已提交 |
| `raw_x402/2026-08-21.json` → `raw_x402/2026-08-21.json.gz` | 原地替换：压缩为同名 `.json.gz`、删除原 `.json`，**同一提交**完成 | 已提交 |
| 抓取逻辑 / 精简规则 / 失败策略 / `x402_daily.yml` / 主管线 | **零改动**（任务书红线） | 未触碰 |

提交记录（均已在远端 main）：

```
db00afa  x402 archive 2026-08-21          ← workflow 验证运行自动提交（产出 .json.gz）
d20156b  x402 archive v1.1: gzip storage  ← 本补丁唯一手工提交（脚本 + 原地替换，单一提交）
```

---

## 二、压缩前后大小对比与两次解压核对

**核对 #1（替换前，读取原 `.json`）：**
- services = **2,310**，endpoints = **29,291**（与 meta `total_endpoints` 一致）
- 原文件大小：**4,072,073 字节（3.88MB）**

**核对 #2（替换后，解压 `.json.gz` 逐字段比对）：**
- services = **2,310**，endpoints = **29,291** —— 计数一致 ✅
- `back == doc` 全文档语义相等（round-trip identical）✅
- 新文件大小：**339,320 字节（331.4KB）**，压缩比 **12.00 : 1**
- 压缩格式与 `write_archive` 完全一致：紧凑 JSON（`separators=(',', ':')`）+ `ensure_ascii=False` + gzip 默认压缩等级

**远端终态验证：**
- workflow（run `32466093453`，手动触发）绿色通过，6 步全绿；
- 远端 `raw_x402/` 目录**仅含** `2026-08-21.json.gz`（342,963 字节，为 workflow 重新抓取写入，无未压缩 `.json` 残留）；
- 从 `raw.githubusercontent.com` 下载该文件 → 解压 → `json.load` 解析成功，services=2,310 / endpoints=29,291 ✅。

**体量测算（长期）：** ~330KB/日 → 约 **120MB/年**（v1.0 未压缩为 ~1.4GB/年，降低约 92%）。哨兵阈值 1MB（压缩后）远未触发。

**离线测试补充：** gzip 往返一致性测试通过；用高熵不可压缩数据验证 `::warning::` 哨兵正确触发且照常写入（只叫不咬语义不变）。

---

## 三、CDP Bazaar 单页字段补探（事项二）

方式：1 次请求 `GET /platform/v2/x402/discovery/resources?type=http&limit=100`，只看不存。返回 100 条，完整字段清单已逐级枚举（含 `extensions.bazaar.*` 的 JSON Schema 嵌套）。

### 问题 1：是否有收款地址？
**有。** 字段 `accepts[].payTo`，100/100 条目均携带，值为 EVM 地址（样例 `0x52E29e0d2Aa49bfBfC548C0A9F2196F4aa51f3ea`）；另有语义重叠的 `accepts[].recipient`（本次样本中未取到非空值）。结算资产：99 条为 Base 链 USDC（合约 `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA029`）、1 条为 Solana USDC。

### 问题 2：是否有上架时间？
**没有。** 仅有 `lastUpdated`（最后更新时间，样例 `2026-08-21T14:12:51.814Z`），不存在创建/上架时间字段。若未来需要"服务存活时长"，只能靠本归档自身的每日快照回溯推断——这正是只归档不删除策略的价值。

### 问题 3：与 Agentic.Market 的对齐键？
**`resource`（完整端点 URL）。** 抽样 100 条 Bazaar `resource` 与当日 Agentic.Market 归档的 29,291 个端点 URL 做精确匹配（去除尾部斜杠归一）：**100/100 全部命中**。即两源可按端点 URL 无歧义对齐，无需模糊匹配。

### 补充发现（对未来 R_A 校准有直接价值）
Bazaar 每条含 `quality` 对象：`l30DaysTotalCalls`（30 天总调用次数）、`l30DaysUniquePayers`（30 天独立付款方数）、`lastCalledAt`（最近调用时间）。这是**真实调用量数据**——若未来 x402 目录挂牌价需要用真实成交密度校准 R_A，Bazaar 是目前唯一免 key 的量来源（但全量抓取受 152 请求预算限制，届时需另行拍板抽样方案）。

---

## 四、全部自主决策清单

| # | 决策 | 理由 |
|---|---|---|
| 1 | 压缩核对以 git 中实际文件（29,291 端点）为准，而非我上一轮本地跑的 29,537 | 远端 workflow 重跑（08:29:20Z）覆盖了当日文件；同日两次抓取间存在 ~246 个端点的目录波动（活源 + 分页偏移），属源端现象，如实记录 |
| 2 | 简报撰写前重跑了一次 Bazaar 单页探测（仍 1 次请求、只看不存） | 上轮探针临时文件 `/tmp/cdp_probe.json` 已清理，为保证简报数字精确补测；行为与任务书"一次请求"精神一致 |
| 3 | 哨兵输出采用 `::warning::` Actions 注解格式 | v1.0 是纯 stdout WARNING，v1.1 借压缩改造升级为 GitHub Actions 日志面板可直接定位的注解 |
| 4 | 失败探针文件也统一为 `.json.gz` | 目录内格式一致性，避免读取方做双格式兼容 |
| 5 | Bazaar 补探结论连同 `quality` 字段发现一并写入本简报（不写任何文件进仓库） | 任务书规定"结果只进简报"；`quality` 量数据与未来 R_A 校准直接相关，属应报告的关键情报 |

---

## 五、红线自查

- ✅ 动手前已 `git pull`（fast-forward 至 `1aae76a`）
- ✅ 脚本改动 + 归档替换在**同一提交** `d20156b` 完成
- ✅ 未改抓取逻辑、精简规则、失败策略；未触碰主管线任何文件
- ✅ workflow 手动触发验证绿色，远端目录无未压缩 `.json` 残留
- ✅ 两次解压核对通过（替换前 + 远端下载后）
