# H1 世纪图表 —— N/R 分类书面规则（v0.1）

> 日期：2026-09-02。用途：把 N/R 分组从 9 个试点品类扩展到 CPI 全样本的**归类依据**，
> 作为「理论驱动三分法 + 盲分类」的书面规则，供任何后续复核者按同一规则复现归类。
> 配套实现：`token-parity/h1_build_mapping.py`（分类 + 叶子硬检查 + 层级树生成），
> 单一信息源仍为 `token-parity/category_mapping.csv`（脚本生成后提交，fetch/analyze 只读它）。

---

## 一、归类规则（一句话，可复核）

> **一个 CPI 品类，若其主要成分是「需现场提供、无法预先库存的人力劳动服务」，归 N；
> 若其主要成分是「可数字复制、或可全球贸易/离岸规模化生产的商品与服务」，归 R；
> 两者混合、或主要驱动力来自外生/管制因素、或依据不明的，归「中性」，不强行二选一。**

展开判据（可操作化）：

| 分组 | 判据（满足其一即可入组，否则不强行入组） | 典型域 |
|---|---|---|
| **N（难自动化·稀缺迁移上涨）** | ① 需现场提供、无法预先库存的人力劳动服务；② 受知识产权/牌照/管制保护的稀缺供给 | 医疗照护、教育、托儿、住房服务、个人服务、维修保养、保险、外食、公共交通 |
| **R（可自动化·丰裕化下跌）** | ① 可数字复制；② 可全球贸易/离岸规模化生产的商品与服务 | 电子硬件、软件、通信/影音、家电家具、服装鞋类、玩具、运动器材、汽车零配件 |
| **中性** | N/R 判据混合，或主要驱动力为外生（食品/能源）、管制（公用事业）、税/费、奢侈品/位置性与可贸易冲突的 | 食品、能源、公用事业、烟酒、珠宝手表、航空 |
| **排除** | 非「品类」的统计集合：大类、交叉聚合（`less X` 类）、含已入组子项的聚合层、基准序列 | SA0、SAH、SAM、SACL*、SAS、SETG… |

**硬约束：不强行二选一。** 依据不足的品类一律「中性」，宁可缩小检验集、不可污染分组。

---

## 二、盲分类协议（防「为结果好看而分类」）

1. **归类先于 δ̂ 计算**：本表 N/R/中性 归类完成并提交的时间戳，**早于** `h1_analyze.py` 首次对这些新序列产出 δ̂（届时本仓库尚未拉到新序列价格数据，归类者客观上不可能看到结果）。
2. **时间戳留痕**：归类数据的提交 commit message 注明「盲分类完成时间」，与后续「数据拉取」「δ̂ 计算」的 commit 分离。
3. 与教科书排除同一纪律（说明书 A 排除决定早于显著性增强发现，无因果关联）。

---

## 三、老品类不动（红线）

既有 12 行（11 已核实 + 1 降级 SEEA）的 `series_id / 层级 / 状态 / H1分组` **原样保留，不因扩展重分类**：

- N：SEMD01、SEEB01、SAM2、SEEB03、SEHC01（5）
- R：SEED03、SEEE02、SERE01、SERA01（4）
- 中性：SAF；基准：SA0；排除：SEEA（降级）

其中 **SAM2（医疗护理服务）是聚合层**，且 ⊃ SEMD01（医院）。这是「老品类不动」的已知例外——
嵌套 caveat 保留（对应 AEI 原图两条线的逐行复现背书优先于统计纯度）。新增品类不制造新的嵌套。

---

## 四、明细优先 + 叶子硬检查（程序化，不靠肉眼）

1. 新扩展品类进入 N/R 检验集的前提 = 是 BLS cu.item 树中的**叶子**（无子项）。
2. 含子项的聚合层（如 SETG ⊃ SETG01/02/03、SEHC ⊃ SEHC01、SERE ⊃ SERE01/02/03）→ 归「排除」，不重复入组。
3. **该判定由 `h1_build_mapping.py` 程序化强制**：脚本内置 `HAS_CHILDREN` 聚合集合，
   校验阶段断言「任何 N/R 品类的 item_code 不得出现在 HAS_CHILDREN 中」，违反即 fail loud。
   —— 130 个品类靠人眼过一遍容易漏，必须机器判。

---

## 五、层级树来源与核验链

- **本轮（v0）**：`h1_item_hierarchy.csv`（parent_code / item_code / item_name / has_children / display_level）
  由脚本内置的 BLS CPI 相对重要性结构生成，标注 `v0 待核验`。
- **本地无法直连 BLS**（download.bls.gov / api.bls.gov 均 403），cu.item 完整文件须由
  **GitHub Action（runner 在美国）直连拉取**，随后用权威 cu.item 重新生成层级树并复跑叶子校验，
  覆盖 v0 中任何人工推断错误。
- 新品类 `状态` 先设 **`待核实`**（fetch 层只抓 `已核实`），待 Action 拉取 cu.series 验证
  seriesID 存在性 + 数据起始年份后，再逐批翻 `已核实`。

---

## 六、后续口径（本条为预告，非本轮任务）

- ex-HI（剔除健康保险）口径：设计成 `h1_analyzed.json` 的 **`adjustments[]` 可扩展数组**
  （每项含 name / reason / excluded_subcomponent / delta_pp_per_year / method），
  不孤零零加一个字段。SEME（健康保险）随本轮扩展正式归档。
- 判别性回归数据源（CE 调查 + 产业生产率）→ 任务②，元数据挂在本轮扩展后的映射表上。
