# O2（Λ′ 净利率变体）方案 — v0.1（供项目所有者审阅）

> **状态**：方案草案，未立项、未动代码。确认后才进入执行。
> **对应路线图**：O2（Λ′ 利润率版本，README 升级路线第 1 条）
> **缓解局限**：L1（Λ 是毛收入比不是利润比）
> **对标关系**：CHR（Hans Royal）的净口径公式 CHR_w = (R_w − C_non-elec) / (1 + m)，与 TEPI 毛口径的直接可比项。

---

## 1. 动机

- TEPI 的 Λ=RA/RM 是**毛收入比**（L1 自述局限）：推理侧成本大头是 GPU 折旧而非电费，均衡应比较全成本利润率。
- CHR 已做净口径：扣除非电力成本五块（冷却、网络、地产、劳动力、硬件摊销）+ 30% 必需回报率。TEPI 落地 Λ′ 后即可与 CHR **同口径直接对照**，补齐"国外对标"的最后一块。

## 2. 定义设计（核心决策点一：双侧扣 or 单侧扣）

### 选项 α：双侧净（推荐）

```
RA′ = RA − C_non-elec,A        （推理侧每 kWh 毛收入 − 非电力成本）
RM′ = RM − C_elec,M ...        （挖矿侧同理扣成本）
Λ′  = RA′ / RM′
```

- **优点**：真正的"利润口径套利比"，与 Λ（毛口径）构成完整对照；挖矿侧成本结构公开充分（上市矿企财报按季度披露电费占比）。
- **缺点**：两侧都要校准，参数多。

### 选项 β：单侧净（贴近 CHR）

```
RA′ = (RA − C_non-elec,A) / (1 + m)    （直接复刻 CHR 公式）
Λ′  = RA′ / RM
```

- **优点**：与 CHR 完全同公式，可比性最强；参数少一半。
- **缺点**：RM 仍是毛口径，"净/毛"混比在经济学上不严格。

**推荐 α**，理由：Λ′ 的目的是回答"净口径下能量套利还剩多少倍"，双侧净才自洽；CHR 对照可以用 α 的 RA′ 单独与 CHR_w 比较，不损失可比性。

## 3. 成本结构（决策点二：参数来源）

推理侧 C_non-elec 五块（沿用 CHR 分类）：

| 块 | 含义 | TEPI 数据源建议 |
|---|---|---|
| 硬件摊销 | GPU/服务器折旧（成本大头） | 上市 GPU 云财报（CoreWeave、Nebius、Crusoe 披露）折旧 ÷ 收入 |
| 冷却 | 数据中心散热（部分已含 PUE） | 财报 + Cushman&Wakefield/JLL 设施报告 |
| 网络 | 带宽与互联 | 财报运营成本项 |
| 地产 | 机柜租金/自建摊销 | 财报 + 设施报告 |
| 劳动力 | 运维人员 | 财报 SG&A 分摊 |

挖矿侧 C_M：上市矿企财报（Marathon Digital、Riot、CleanSpark、Core Scientific）"电费占收入比"与运营成本占比，季度披露、公开充分。

**两种校准路径：**

| 路径 | 做法 | 时效 | 精度 |
|---|---|---|---|
| **财报校准（推荐）** | 每季度读 4–6 家财报，算成本占收入比 c_A、c_M，更新 Λ′ | 滞后一季 | 高 |
| CHR 式分块假设 | 按 CHR 的 C_non-elec 分档（tier 假设值）+ m=0.30 | 即时 | 低（CHR 自己也承认"估值粗糙，硬件摊销敏感性未充分披露"） |

**推荐财报校准**：与 README 既有规划（"Λ' 需要矿企/云厂商财报做季度校准"）一致，且避开 CHR 假设值的不透明问题。

## 4. 频率与序列治理（决策点三）

- **季频**，与日频主序列**并行、永不混同**（参照历史回填 source=backfill 的隔离原则）。
- 存储：`docs/lambda_prime_series.csv`（独立文件，不动 `parity_series.csv`），列：`quarter, RA, RA_prime, RM, RM_prime, Lambda, Lambda_prime, c_A, c_M, source, notes`。
- **对日频序列零影响**：Λ′ 不进入 docs/index.html 大屏、不进入主 CSV；只在方法论文档与季度报告中引用。

## 5. 落地形式（决策点四）

| 阶段 | 动作 | 产出 |
|---|---|---|
| **阶段一（推荐先行）** | 纯文档试算：用最近一季（2026 Q2/Q3）财报手工算一次 Λ′ | `docs/lambda_prime.md`（口径定义 + 首次试算 + 敏感性） |
| 阶段二（阶段一验证口径后再决定） | 代码化：独立脚本 `lambda_prime.py`（非改 `parity_index.py`），季度手动触发 | 自动化计算 |

**推荐先阶段一**：先验证口径与数据可得性，避免过早改代码。

## 6. 风险与预案

| 风险 | 说明 | 预案 |
|---|---|---|
| R1 财报口径异质 | 各厂商成本科目命名不一，折旧方法不同 | 登记每家的科目映射表，changelog 留痕 |
| R2 样本代表性 | CoreWeave 等是"租 GPU"业务，与模型厂商自持算力成本结构不同 | c_A 取多家中位数并在文档标注样本局限 |
| R3 阶段一数据量 | 首次试算需读 4–6 份财报 | 我逐家摘取关键行，整理成表供你复核 |
| R4 混淆风险 | 读者误把 Λ′ 当 Λ 的"更准版本" | 方法论中明确：Λ 是日频毛口径主序列，Λ′ 是季频净口径对照，二者回答不同问题 |

## 7. 待你确认的四个决策点

1. **定义**：α 双侧净（推荐）还是 β 单侧贴 CHR？
2. **参数来源**：财报校准（推荐）还是 CHR 式分块假设？
3. **落地形式**：先阶段一文档试算（推荐）还是直接代码化？
4. **是否立项**：本轮就做，还是推迟到 P0/其他优先项之后？

---

**确认后我执行阶段一**：逐家读取财报摘取成本行，产出 `token-parity/docs/lambda_prime.md` 首次试算稿，供你复核后再谈代码化。
